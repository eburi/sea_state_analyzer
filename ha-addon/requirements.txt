# Python dependencies for boat_state HA App.
# numpy, scipy, pandas, matplotlib are installed via apk (Alpine packages)
# for faster build and smaller image size on aarch64.

httpx>=0.27
websockets>=12.0
pyarrow>=15.0
smbus2>=0.4
